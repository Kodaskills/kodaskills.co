ci:
  assert:
    assertions:
      first-contentful-paint:
        - error
        - minScore: 0.9
  collect:
    headful: false,
    url: https://kodaskills.co
    # staticDistDir: ./dist/
    settings:
      chromeFlags: --disable-gpu --no-sandbox --no-zygote
