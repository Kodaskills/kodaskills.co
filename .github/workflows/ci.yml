name: Continuous Integration

on:
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "!src/components/astrobook/**"
      - "!**.stories.*"
      - "public/**"
      - "uno.config.ts"
      - "astro.config.*"
      - "package.json"
      - "tsconfig.json"
    
  # push:
  #   branches: [main]
  #   paths:
  #     - "src/**"
  #     - "!src/components/astrobook/**"
  #     - "!**.stories.*"
  #     - "public/**"
  #     - "uno.config.ts"
  #     - "astro.config.*"
  #     - "package.json"
  #     - "tsconfig.json"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  BUN_VERSION: ${{ vars.BUN_VERSION }}
  # TEMP: allow Node20 actions after runners switched to Node24 (remove fall 2026)
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

permissions:
  contents: read
  checks: write

jobs:
  lint:
    name: 🔍 Linting and Formatting
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v6

      - name: 📦 Setup
        uses: ./.github/actions/setup-bun

      - name: 🔍 Biome lint
        run: bun biome check .

  unused-deps:
    name: ✂️ Dependencies Unused
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v6

      - name: 📦 Setup
        uses: ./.github/actions/setup-bun

      - name: ✂️ Knip
        run: bun knip

  astro-check:
    name: 🧪 Astro Type Checking
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v6

      - name: 📦 Setup
        uses: ./.github/actions/setup-bun

      - name: 🧪 TypeScript check
        run: bun astro check

  i18n:
    name: 🌍 Verify Translations
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v6

      - name: 📦 Setup
        uses: ./.github/actions/setup-bun

      - name: 🌍 Lingui compile (strict)
        run: bun lingui compile --strict

  build:
    name: 🏗️ Build Astro
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v6

      - name: 📦 Setup
        uses: ./.github/actions/setup-bun

      - name: 🔨 Build
        run: bun run build

      - name: 📤 Upload dist artifact
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 1
